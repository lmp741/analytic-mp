# Инструкция по загрузке проекта на GitHub

## Шаг 1: Установка Git (если не установлен)

### Windows:
1. Скачайте Git с [git-scm.com](https://git-scm.com/download/win)
2. Установите с настройками по умолчанию
3. Перезапустите терминал/PowerShell

### Проверка установки:
```bash
git --version
```

## Шаг 2: Инициализация Git репозитория

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация репозитория
git init

# Добавление всех файлов (кроме .env.local и node_modules)
git add .

# Первый коммит
git commit -m "Initial commit: Analytic MP project"
```

## Шаг 3: Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Заполните:
   - **Repository name:** `analytic-mp` (или любое другое имя)
   - **Description:** "Analytics for WB and Ozon marketplaces"
   - **Visibility:** Public или Private (на ваш выбор)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть)
5. Нажмите **"Create repository"**

## Шаг 4: Подключение локального репозитория к GitHub

GitHub покажет инструкции. Выполните в терминале:

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/analytic-mp.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используйте Personal Access Token (не пароль)
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- Права: `repo` (полный доступ к репозиториям)

## Шаг 5: Проверка

Зайдите на GitHub и убедитесь, что все файлы загружены:
- ✅ `package.json`
- ✅ `app/` папка
- ✅ `lib/` папка
- ✅ `components/` папка
- ✅ `supabase/migrations/`
- ✅ `.gitignore` (должен быть)
- ❌ `.env.local` (НЕ должен быть виден - он в .gitignore)

## Шаг 6: Подключение к Vercel

Теперь, когда код на GitHub:

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New Project"**
3. Выберите **"Import Git Repository"**
4. Авторизуйтесь через GitHub (если нужно)
5. Выберите репозиторий `analytic-mp`
6. Нажмите **"Import"**

### Настройка проекта в Vercel:

1. **Framework Preset:** Next.js (определится автоматически)
2. **Root Directory:** `./` (по умолчанию)
3. **Build Command:** `npm run build` (автоматически)
4. **Output Directory:** `.next` (автоматически)

### Добавление Environment Variables:

**КРИТИЧЕСКИ ВАЖНО!**

1. В разделе **"Environment Variables"** добавьте:
   ```
   NEXT_PUBLIC_SUPABASE_URL = ваш_supabase_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY = ваш_supabase_anon_key
   ```

2. Выберите окружения:
   - ✅ Production
   - ✅ Preview  
   - ✅ Development

3. Нажмите **"Save"**

### Деплой:

1. Нажмите **"Deploy"**
2. Дождитесь завершения (обычно 2-3 минуты)
3. Готово! 🎉

## Альтернативный способ (через GitHub Desktop)

Если не хотите использовать командную строку:

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите в аккаунт
3. File → Add Local Repository → выберите папку проекта
4. Publish repository → выберите имя и создайте на GitHub
5. Код автоматически загрузится

## Важные замечания:

- ✅ `.env.local` уже в `.gitignore` - он НЕ загрузится на GitHub
- ✅ `node_modules` тоже в `.gitignore` - Vercel установит зависимости автоматически
- ✅ Все переменные окружения нужно добавить в Vercel Dashboard
- ✅ После первого деплоя каждый push в `main` будет автоматически деплоить новую версию

## Troubleshooting:

### Ошибка: "fatal: not a git repository"
```bash
git init
```

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/analytic-mp.git
```

### Ошибка: "Permission denied"
- Проверьте, что используете Personal Access Token, а не пароль
- Убедитесь, что токен имеет права `repo`

### Ошибка при push: "refusing to merge unrelated histories"
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```
