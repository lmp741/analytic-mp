# Analytic MP - Аналитика маркетплейсов

Production-ready веб-приложение для аналитики метрик маркетплейсов Wildberries и Ozon с использованием Supabase и деплоем на Vercel.

## Возможности

- 📊 **Загрузка и парсинг отчетов** (WB и Ozon) с поддержкой XLSX/CSV
- 🔍 **Dry-run режим** для валидации файлов перед импортом
- 📈 **Дашборды** для каждого маркетплейса с динамикой WoW
- 🎯 **Summary страница** с автоматическим определением сигналов
- 🧪 **AB-тесты** с отслеживанием baseline метрик
- 📅 **Месячная сводка** по маркетплейсам
- ⚙️ **Настройки** порогов и параметров

## Технологии

- **Next.js 14** (App Router) + TypeScript
- **Tailwind CSS** + shadcn/ui компоненты
- **Supabase** (PostgreSQL + Auth + RLS)
- **SheetJS** (xlsx) для парсинга файлов
- **Recharts** для графиков
- **TanStack Table** для таблиц

## Требования

- Node.js 18+ и npm
- Аккаунт Supabase
- Аккаунт Vercel (для деплоя)

## Установка

### 1. Клонирование и установка зависимостей

```bash
npm install
```

### 2. Настройка Supabase

1. Создайте новый проект в [Supabase](https://supabase.com)
2. Перейдите в SQL Editor и выполните миграции:
   - `supabase/migrations/001_initial_schema.sql`
   - `supabase/migrations/002_rls_policies.sql`
   - `supabase/migrations/003_disable_rls.sql` (для single-user v1)

3. Получите URL и Anon Key из Settings > API

### 3. Настройка переменных окружения

Создайте файл `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## Деплой на Vercel

### 1. Подготовка

Убедитесь, что:
- Все миграции Supabase выполнены
- Переменные окружения настроены локально

### 2. Деплой

1. Подключите репозиторий к Vercel
2. В настройках проекта добавьте переменные окружения:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. Деплой будет выполнен автоматически

### 3. Настройка Supabase Auth (опционально)

Для production рекомендуется включить аутентификацию:

1. В Supabase Dashboard: Authentication > Providers
2. Включите Email provider
3. Обновите RLS policies при необходимости

## Структура проекта

```
├── app/                    # Next.js App Router страницы
│   ├── upload/            # Страница загрузки файлов
│   ├── wb/                # WB дашборд
│   ├── ozon/              # Ozon дашборд
│   ├── summary/           # Summary с сигналами
│   ├── ab-tests/          # AB тесты
│   ├── month/             # Месячная сводка
│   └── settings/          # Настройки
├── components/            # React компоненты
│   └── ui/                # shadcn/ui компоненты
├── lib/
│   ├── parsers/           # Парсеры WB и Ozon
│   ├── supabase/          # Supabase клиенты
│   └── utils/              # Утилиты (парсинг, форматирование)
├── supabase/
│   └── migrations/        # SQL миграции
└── public/                # Статические файлы
```

## Использование

### Загрузка данных

1. Перейдите на `/upload`
2. Загрузите файл WB (лист "Товары")
3. Загрузите файл Ozon (лист "По товарам")
4. Используйте "Dry-run" для валидации
5. Нажмите "Import to database" для импорта

### Просмотр дашбордов

- `/wb` - дашборд Wildberries
- `/ozon` - дашборд Ozon
- Клик по артикулу открывает детальную информацию

### Summary и сигналы

- `/summary` - показывает товары с сигналами (падение метрик)
- Сигналы определяются на основе порогов из Settings
- Можно добавить товар в AB-тесты прямо из Summary

### AB-тесты

- `/ab-tests` - список активных тестов
- Baseline метрики сохраняются при создании
- WoW дельты рассчитываются автоматически
- "Remove from AB list" не удаляет данные, только деактивирует тест

### Настройки

- `/settings` - редактирование порогов
- Пороги вводятся в процентах, хранятся как дроби (0..1)
- Изменения применяются к Summary сразу после сохранения

## Особенности парсинга

### WB Parser
- Ищет лист "Товары" (fuzzy match)
- Определяет период из ячеек
- Толерантное сопоставление колонок
- Валидация артикулов (GFA-5200, GFM-300)

### Ozon Parser
- Обрабатывает двухстрочные заголовки
- Агрегирует дубликаты по артикулу
- Вычисляет CTR = visits / impressions
- Определяет период из первых 20 строк

## Безопасность

- RLS (Row Level Security) отключен для single-user v1 через `003_disable_rls.sql`
- Аутентификация через Supabase Auth (опционально)
- Service role key не используется в клиенте
- Все операции проверяют права доступа

## Применение миграций (Supabase)

Можно применить миграции двумя способами:

1. **SQL Editor**: последовательно выполните файлы из `supabase/migrations/`.
2. **Supabase CLI** (если используется локально):
   ```bash
   supabase db push
   ```

Если таблицы не применены, /upload покажет предупреждение **DB schema not applied**.

## Тестирование

```bash
npm test
```

## Лицензия

MIT

## Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Проверьте логи Supabase
3. Используйте "Download diagnostics JSON" на странице загрузки
