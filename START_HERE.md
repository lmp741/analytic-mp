# 🚀 Быстрый старт: GitHub → Vercel

## 📋 Что нужно сделать:

### 1️⃣ Установите Git (если еще не установлен)
- Скачайте: https://git-scm.com/download/win
- Установите с настройками по умолчанию

### 2️⃣ Создайте репозиторий на GitHub
1. Зайдите на [github.com](https://github.com) и войдите
2. Нажмите **"+"** → **"New repository"**
3. Имя: `analytic-mp`
4. **НЕ** ставьте галочки (README, .gitignore, license)
5. Нажмите **"Create repository"**

### 3️⃣ Загрузите код на GitHub

Откройте PowerShell/терминал в папке проекта и выполните:

```bash
# Инициализация
git init

# Добавление файлов
git add .

# Коммит
git commit -m "Initial commit"

# Подключение к GitHub (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/analytic-mp.git

# Переименование ветки
git branch -M main

# Загрузка на GitHub
git push -u origin main
```

**Если попросит авторизацию:**
- Используйте Personal Access Token (не пароль)
- Создайте: GitHub → Settings → Developer settings → Personal access tokens
- Права: `repo`

### 4️⃣ Подключите к Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. **"Add New Project"** → выберите репозиторий `analytic-mp`
3. **Добавьте Environment Variables:**
   - `NEXT_PUBLIC_SUPABASE_URL` = ваш Supabase URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = ваш Supabase Anon Key
4. Нажмите **"Deploy"**

### 5️⃣ Готово! 🎉

---

## 📚 Подробные инструкции:

- **GITHUB_SETUP.md** - детальная инструкция по GitHub
- **QUICK_DEPLOY.md** - быстрый деплой на Vercel
- **DEPLOY.md** - полная документация по деплою

---

## ⚠️ Важно:

- ✅ `.env.local` НЕ загрузится на GitHub (он в .gitignore)
- ✅ Все переменные окружения нужно добавить в Vercel
- ✅ Vercel автоматически установит зависимости при деплое
