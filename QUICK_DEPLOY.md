# Быстрый деплой на Vercel

## ✅ Что уже готово:
- ✓ Проект настроен для Vercel
- ✓ `vercel.json` создан
- ✓ Все зависимости в `package.json`

## 🚀 Шаги для деплоя:

### 1. Проверьте .env.local
Убедитесь, что файл содержит:
```
NEXT_PUBLIC_SUPABASE_URL=ваш_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_key
```

### 2. Зайдите на vercel.com
1. Откройте [vercel.com](https://vercel.com)
2. Войдите в аккаунт (или создайте через GitHub)

### 3. Создайте новый проект
1. Нажмите **"Add New Project"**
2. Подключите ваш Git репозиторий
3. Выберите проект `Analytic_MP`

### 4. Добавьте Environment Variables
**ВАЖНО!** В настройках проекта:

1. Перейдите в **Settings → Environment Variables**
2. Добавьте две переменные:
   - `NEXT_PUBLIC_SUPABASE_URL` = ваш Supabase URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = ваш Supabase Anon Key
3. Выберите все окружения (Production, Preview, Development)
4. Нажмите **Save**

### 5. Деплой
1. Нажмите **"Deploy"**
2. Vercel автоматически:
   - ✅ Установит зависимости (`npm install`)
   - ✅ Соберет проект (`npm run build`)
   - ✅ Задеплоит приложение

### 6. Готово! 🎉
После деплоя вы получите URL вашего приложения.

---

## 📝 Примечания:

- **Vercel автоматически устанавливает зависимости** - вам не нужно ничего делать вручную
- После первого деплоя каждый push в `main` будет автоматически деплоить новую версию
- Pull requests получат свой preview URL

## ❓ Если что-то пошло не так:

1. Проверьте логи сборки в Vercel Dashboard
2. Убедитесь, что переменные окружения добавлены правильно
3. Проверьте, что Supabase миграции выполнены
4. См. подробную инструкцию в `DEPLOY.md`
