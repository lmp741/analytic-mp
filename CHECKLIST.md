# ✅ Чеклист перед деплоем на Vercel

## 1. Проверка .env.local

Убедитесь, что файл `.env.local` существует и содержит:

```env
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key_здесь
```

**Где найти эти значения:**
- Зайдите в Supabase Dashboard → Settings → API
- `NEXT_PUBLIC_SUPABASE_URL` = Project URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` = anon/public key

## 2. Проверка Supabase

- [ ] Миграции выполнены (`supabase/migrations/001_initial_schema.sql`)
- [ ] RLS policies применены (`supabase/migrations/002_rls_policies.sql`)
- [ ] Settings row создан (id=1)

## 3. Локальная проверка (опционально)

```bash
npm install
npm run build
```

Если сборка проходит успешно - готово к деплою!

## 4. Деплой на Vercel

### Вариант A: Через Dashboard (рекомендуется)

1. [ ] Зайдите на [vercel.com](https://vercel.com)
2. [ ] Нажмите "Add New Project"
3. [ ] Подключите Git репозиторий
4. [ ] **Добавьте Environment Variables:**
   - [ ] `NEXT_PUBLIC_SUPABASE_URL`
   - [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY`
5. [ ] Нажмите "Deploy"

### Вариант B: Через CLI

```bash
npm i -g vercel
vercel login
vercel
```

## 5. После деплоя

- [ ] Откройте URL приложения
- [ ] Проверьте главную страницу
- [ ] Попробуйте загрузить тестовый файл на `/upload`
- [ ] Проверьте, что данные сохраняются в Supabase

---

## ⚠️ Важно помнить:

1. **Vercel автоматически установит зависимости** - вам не нужно ничего делать
2. **Не коммитьте .env.local** - он уже в .gitignore
3. **Все переменные окружения** должны быть в Vercel Dashboard
4. После первого деплоя каждый push в main будет автоматически деплоить новую версию

## 📚 Документация:

- Подробная инструкция: `DEPLOY.md`
- Быстрая инструкция: `QUICK_DEPLOY.md`
- Общий README: `README.md`
